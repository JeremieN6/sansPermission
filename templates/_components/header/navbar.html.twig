<nav class="flex items-center justify-between py-4 px-8 max-w-7xl mx-auto">
    <div class="text-2xl font-bold text-white">
        <img src="{{asset('assets/images/logo-no-bg.png')}}" width="100px" height="100px" loading="lazy" alt="Logo Sans Permission">
    </div>
    
    <div class="flex space-x-8">
        <a href="{{path('app_main')}}" class="text-white font-medium transition duration-300 hover:text-green-400 {{ app.request.get('_route') == 'home' ? 'text-green-400' : '' }}">Accueil</a>
        <a href="#episodes_ancre" class="text-white font-medium transition duration-300 hover:text-green-400">Episodes</a>
        <a href="{{path('app_aboutus')}}" class="text-white font-medium transition duration-300 hover:text-green-400">A Propos</a>
        <a href="#" class="text-white font-medium transition duration-300 hover:text-green-400">Quiz</a>
        {% if is_granted('ROLE_ADMIN') %}
            <div class="text-white font-medium">•</div>
            <a href="{{path('admin')}}" class="text-white font-medium transition duration-300 hover:text-green-400">Admin</a>
        {% endif %}
    </div>

    <div>
        <a href="{{path('app_contact')}}" class="px-6 py-2 rounded-full border border-white/20 text-white hover:bg-white/10 transition duration-300">
            Contact
        </a>
    </div>
</nav> 