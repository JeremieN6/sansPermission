<div class="relative bg-[#002626] overflow-hidden">
    <div class="max-w-7xl mx-auto px-8 pt-16 pb-24">
        <div class="grid grid-cols-2 gap-12">
            <!-- Left Column -->
            <div class="flex flex-col justify-center">
                <h1 class="text-5xl font-bold text-white italic leading-2 mb-8">
                    Entreprendre sans limites <br> Apprendre sans permission
                </h1>
                
                <p class="text-lg text-white/80 mb-12 max-w-xl">
                    {{channel.description}}
                </p>

                <div>
                    <a href="{{youtube_channel_url}}" class="inline-block px-8 py-4 bg-[#4ADE80] text-black font-medium rounded-full hover:bg-[#22c55e] transition duration-300" target="_blank">
                        Voir la chaîne
                    </a>
                </div>
            </div>

            <!-- Right Column -->
            <div class="relative">
                <div class="bg-white rounded-3xl overflow-hidden">
                    <img src="{{asset('assets/images/hero-img.webp')}}" alt="Podcast Host" class="w-full h-full object-cover">
                </div>
            </div>
        </div>
    </div>
</div> 