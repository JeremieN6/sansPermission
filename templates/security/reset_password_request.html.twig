{% extends 'base_login.html.twig' %}

{% block title %}Réinitialisation de votre mot de passe | Sans Permission{% endblock %}

{% block body %}
<div class="flex items-center justify-center bg-gray-100">
    <div class="flex w-full bg-white shadow-md overflow-hidden h-screen">
        {# Section de gauche avec l'illustration - cachée en mobile #}
        <div class="hidden sm:flex w-1/3 xl:w-1/2 bg-[#450996] container-logo flex justify-center items-center">
            <img src="{{asset('assets/images/logo-no-bg.png')}}" alt="" class="container-logo w-1/3">
        </div>

        {# Section de droite avec le formulaire #}
        <div class="flex items-center justify-center flex-col md:w-2/3 xl:w-1/2 p-8 space-y-6">
            <div class="container-title">
                <h1 class="text-3xl font-bold text-center text-gray-900">Réinitialisation de mot de passe 1/2</h1>
            </div>
            {{ form_start(requestPassForm, {
            attr: {'class': 'flex flex-col w-full'}
            }) }}

            {{ form_row(requestPassForm.email, {
                attr:{'class': 'mb-3 block w-full border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500 p-2'},
                label_attr: {'class': 'block text-sm font-medium text-gray-700'}
            }) }}
            <button class="w-full py-2 px-4 bg-[#450996] text-white font-medium rounded-md hover:bg-[#4B25B6] transition duration-300" type="submit">Valider</button>
            {{ form_end(requestPassForm) }}
        </div>
    </div>
</div>

{% endblock %}